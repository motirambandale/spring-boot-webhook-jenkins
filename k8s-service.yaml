apiVersion: v1
kind: Service
metadata:
  name: spring-boot-webhook-jenkins-service
spec:
  type: NodePort
  selector:
    app: spring-boot-webhook-jenkins-app
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30080